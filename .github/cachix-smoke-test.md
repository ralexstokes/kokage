# Cachix CI smoke test

This temporary file exists only to trigger an ordinary pull-request CI run and
verify that the shared Cachix cache is restored successfully.

A follow-up commit triggers a second run with the pull request's Cargo cache warm.
